<% _.each(locations, function(location) { %>
<div>
	<b><%= location.department %></b>
	<%= (location.label) ? "<p>"+location.label+"</p>" : "" %>
	<p><%= location.availableitems %></p>
</div>
<% }); %>