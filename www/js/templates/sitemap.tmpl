<% _.each(items, function(item) { %>
<li class="ui-screen-hidden">
	<a href="#<% print(encodeURIComponent(JSON.stringify(item.context))) %>">
		<h3><%= item.name %></h3>
		<p><%= item.description %></p>
	</a>
</li>
<% }); %>